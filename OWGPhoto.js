class OWGPhoto {
    constructor(id,vend,width,height,url) {
      this.id     = id;
      this.vend   = vend;
      this.width  = width;
      this.height = height;
      this.url    = url;
    };
  }
  
export default OWGPhoto;